const course = "Python for Beginners";
//console.log(course[0:4]) // only works in python
//console.log(course[-1]) // only in python
console.log(course.slice(0,4)) // only works in python
console.log(10**3)
console.log(Math.round(2.5))
const isHot = true
if(isHot){
    console.log("It's a hot day")
}
const numbers =[6, 2, 6, 2, 2]
numbers.forEach(num => {
    console.log('x' * num)
})