# https://pypi.org - where you can find third party python packages. you can also publish your own package

# openpyxl - package for working with excel spreadsheets
# pip: a tool that comes with python installation and we use it to install and remove python packages registered on pypi.org
# packges we installed with pip are stored in site-packages
# in openpyxl package we have several sub packages and each sub packages we have several modules
